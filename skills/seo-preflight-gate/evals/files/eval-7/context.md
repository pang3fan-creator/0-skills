# Context: eval-7 (missing-input-sitemap)

**Target production URL:** https://example.com/tools/schema-generator

This is a single-language (English) tool page. hreflang does not apply.

**HTTP status:** not provided in this context. No status data is available.

**Important:** NO sitemap file and NO internal pages were provided for this eval.
Only the target URL and the preview server-side HTML (page.html) are available.

The page itself is clean: correct self-referencing canonical, `index, follow`,
one title, one H1, and body text all server-rendered.

Because the sitemap and internal-page inputs are absent, checks 6 and 7 cannot be
verified from the provided inputs — they should be reported as N/A, not FAIL.
